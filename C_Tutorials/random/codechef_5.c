#include<stdio.h>
int main(void){
    int t;
    scanf("%d",&t);
    while (t--)
    {
        int x,a,b,d,n,a,v[n];
        scanf("%d",&x);
        scanf("%d",&a);
        scanf("%d",&b);

        for (int d = 1; d <= n; d++)
        {
            if (n%d==0)
            {
                v[d-1]=d;
            }
            
        }
        for (int i = 0; i < n; i++)
        {
            /* code */
        }
        

    }
    
}